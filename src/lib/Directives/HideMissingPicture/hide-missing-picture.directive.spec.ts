import { HideMissingPictureDirective } from './hide-missing-picture.directive';

describe('HideMissingPictureDirective', () => {
  it('should create an instance', () => {
    const directive = new HideMissingPictureDirective();
    expect(directive).toBeTruthy();
  });
});
